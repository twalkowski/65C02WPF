<Window x:Class="_65C02WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:_65C02WPF"
        Title="65C02 Emulator" Height="450" Width="840" BorderBrush="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}">
    
    <Window.Resources>
        <local:IntToHexConverter x:Key="intHexConverter"/>
        <local:ByteToHexConverter x:Key="byteHexConverter"/>
    </Window.Resources>
    
    <Grid Margin="20,0,0,20" RenderTransformOrigin="0.525,0.251" MinWidth="800" MinHeight="400" HorizontalAlignment="Left" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="80*" />
            <RowDefinition Height="319*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="Auto" MinWidth="59"/>
            <ColumnDefinition Width="Auto" MinWidth="517"/>
        </Grid.ColumnDefinitions>

        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,20,0,0">
            <Button x:Name="Reset" Content="Reset" HorizontalAlignment="Left" Margin="8,4,4,4" VerticalAlignment="Top" Width="75" Click="Reset_Click" RenderTransformOrigin="-0.467,0.5"/>
            <Button x:Name="Step" Content="Step" Margin="8,4,4,4" Width="75" Click="Step_Click" RenderTransformOrigin="0.533,3.25" HorizontalContentAlignment="Center"/>
        </StackPanel>

        <StackPanel Margin="20,20,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Grid.Row="1" Grid.Column="0">
            <StackPanel Orientation="Horizontal" Margin="8,4,4,4" HorizontalAlignment="Left" VerticalAlignment="Top">
                <TextBlock TextWrapping="Wrap" Text="Accumulator: " HorizontalAlignment="Right" Width="100" Margin="0" Height="16" />
                <TextBox TextWrapping="Wrap" Text="{Binding Accumulator, Converter={StaticResource intHexConverter}, Mode=OneWay}" Style="{StaticResource ShowHex}" Margin="0" VerticalAlignment="Top" IsReadOnly="True"/>
            </StackPanel>
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Orientation="Horizontal" Margin="8,4,4,4">
                <TextBlock TextWrapping="Wrap" Text="X Register: " HorizontalAlignment="Right" Width="100"/>
                <TextBox TextWrapping="Wrap" Text="{Binding XRegister, Converter={StaticResource byteHexConverter}, Mode=OneWay}" Style="{StaticResource ShowHex}" IsReadOnly="True"/>
            </StackPanel>
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Orientation="Horizontal" Margin="8,4,4,4">
                <TextBlock TextWrapping="Wrap" Text="Y Register: " HorizontalAlignment="Right" Width="100"/>
                <TextBox TextWrapping="Wrap" Text="{Binding YRegister, Converter={StaticResource intHexConverter}, Mode=OneWay}" Style="{StaticResource ShowHex}" IsReadOnly="True"/>
            </StackPanel>
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Orientation="Horizontal" Margin="8,4,4,4">
                <TextBlock TextWrapping="Wrap" Text="Stack Pointer: " HorizontalAlignment="Right" Width="100"/>
                <TextBox TextWrapping="Wrap" Text="{Binding StackPointer, Converter={StaticResource intHexConverter}, Mode=OneWay}" Style="{StaticResource ShowHex}" IsReadOnly="True"/>
            </StackPanel>
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Orientation="Horizontal" Margin="8,4,4,4">
                <TextBlock TextWrapping="Wrap" Text="Program Counter: " Width="100" HorizontalAlignment="Right"/>
                <TextBox TextWrapping="Wrap" Text="{Binding ProgramCounter, Converter={StaticResource intHexConverter}, Mode=OneWay}" Style="{StaticResource ShowHex}" IsReadOnly="True"/>
            </StackPanel>
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Orientation="Horizontal" Margin="8,4,4,4">
                <TextBlock TextWrapping="Wrap" Text="Status Register: " HorizontalAlignment="Right" Width="100"/>
                <TextBox TextWrapping="Wrap" Text="{Binding StatusRegister, Converter={StaticResource byteHexConverter}, Mode=OneWay}"  Style="{StaticResource ShowHex}" IsReadOnly="True"/>
            </StackPanel>
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Orientation="Horizontal" Margin="8,4,4,4">
                <TextBlock TextWrapping="Wrap" Text="Address Bus: " HorizontalAlignment="Right" Width="100"/>
                <TextBox TextWrapping="Wrap" Text="{Binding Address, Converter={StaticResource intHexConverter}, Mode=OneWay}"  Style="{StaticResource ShowHex}" IsReadOnly="True"/>
            </StackPanel>
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Orientation="Horizontal" Margin="8,4,4,4">
                <TextBlock TextWrapping="Wrap" Text="Data Bus: " HorizontalAlignment="Right" Width="100"/>
                <TextBox TextWrapping="Wrap" Text="{Binding Data, Converter={StaticResource intHexConverter}, Mode=OneWay}"  Style="{StaticResource ShowHex}" IsReadOnly="True"/>
            </StackPanel>
        </StackPanel>
        
        <StackPanel Grid.Column="2"  Margin="20,20,0,0"  Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" >
            <TextBlock Text="Page:" Style="{StaticResource LabelStyle}" />
            <TextBox x:Name="Page" Text="{Binding Page, Converter={StaticResource intHexConverter}, Mode=OneWay}" Style="{StaticResource BoxFontStyle}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <Button x:Name="Next" Content="Next Page" Click="Next_Click" BorderBrush="Black" />
            <Button x:Name="Prev" Content="Prev Page" Click="Prev_Click" BorderBrush="Black" />
        </StackPanel>

        <StackPanel  Grid.Row="1" Grid.Column="1" Margin="20,20,0,-3" HorizontalAlignment="Left" VerticalAlignment="Top">
            <StackPanel>
                <CheckBox Content="N" IsChecked="{Binding Nflag, Mode=OneWay}" FontSize="12" FontFamily="Consolas" VerticalAlignment="Top" MinWidth="0" MinHeight="0" IsEnabled="False" />
                <CheckBox Content="V" IsChecked="{Binding Vflag, Mode=OneWay}" FontSize="12" FontFamily="Consolas" VerticalAlignment="Top" MinWidth="0" MinHeight="0" IsEnabled="False" />
                <CheckBox Content="-" IsChecked="True"                         FontSize="12" FontFamily="Consolas" VerticalAlignment="Top" MinWidth="0" MinHeight="0" IsEnabled="False" />
                <CheckBox Content="B" IsChecked="{Binding Bflag, Mode=OneWay}" FontSize="12" FontFamily="Consolas" VerticalAlignment="Top" MinWidth="0" MinHeight="0" IsEnabled="False" />
                <CheckBox Content="D" IsChecked="{Binding DFlag, Mode=OneWay}" FontSize="12" FontFamily="Consolas" VerticalAlignment="Top" MinWidth="0" MinHeight="0" IsEnabled="False" />
                <CheckBox Content="I" IsChecked="{Binding IFlag, Mode=OneWay}" FontSize="12" FontFamily="Consolas" VerticalAlignment="Top" MinWidth="0" MinHeight="0" IsEnabled="False" />
                <CheckBox Content="Z" IsChecked="{Binding ZFlag, Mode=OneWay}" FontSize="12" FontFamily="Consolas" VerticalAlignment="Top" MinWidth="0" MinHeight="0" IsEnabled="False" />
                <CheckBox Content="C" IsChecked="{Binding CFlag, Mode=OneWay}" FontSize="12" FontFamily="Consolas" VerticalAlignment="Top" MinWidth="0" MinHeight="0" IsEnabled="False" />
            </StackPanel>
        </StackPanel>

        <TextBox Text="{Binding HexDump, Mode=OneWay}" Style="{StaticResource BoxFontStyle}" Margin="20,20,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Grid.Column="2" Grid.Row="1" />

    </Grid>
</Window>
